meta {
  name: Rotacja client secret
  type: http
  seq: 4
}

# Nowy secret zwracany jest jednorazowo w odpowiedzi.

post {
  url: {{base_url}}/api/admin/clients/demo-client/rotate-secret
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}
